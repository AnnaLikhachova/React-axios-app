import React from "react";
import MoviesList from '../components/MoviesList'

const MovieSearch = () => (
    <div className="movie-search-nav">
    <h1>MovieSearch</h1>
    <MoviesList/>
    </div>
    );

export default MovieSearch;